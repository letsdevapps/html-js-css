import { test, expect } from '@playwright/test';

test('deve abrir a página index', async ({ page }) => {
  await page.goto('/');

  //await expect(page).toHaveTitle(/.*/);
  await expect(page.locator('title')).toHaveText('Template Local');
});